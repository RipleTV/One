import './pictures.js';
import './audio.js'

import './switcher.js';
import './counter.js';

if (process.env.NODE_ENV !== 'production') {
    console.log('[index.js] Looks like we are in development mode!');
}